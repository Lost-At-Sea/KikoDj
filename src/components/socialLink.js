import React from 'react';

const SocialLink = () => {
  const TagName = this.props.tagName;

  <li>
    <a href={this.props.href} alt={this.props.alt} target="_blank" rel="noreferrer">
      <TagName className="fab"/>
    </a>
  </li>
}

export default SocialLink;